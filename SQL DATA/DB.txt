CREATE TABLE NOTE_MAIN (
  NOTE_NO NUMBER PRIMARY KEY,
  NOTE_RECIPIENT VARCHAR2(255),
  NOTE_SENDER VARCHAR2(255),
  NOTE_TITLE VARCHAR2(255),
  NOTE_CONTENT VARCHAR2(2000),
  NOTE_DATE DATE,
  NOTE_SENDER_STATUS NUMBER,
  NOTE_RECIPIENT_STATUS NUMBER;
);

동물병원 고유번호 ANIMALHOSP_NO -  ANIMALHOSP_NO_SEQ
사업장명 ANIMALHOSP_NAME - STRING
인허가 날짜 LICENSE_DATE - DATE <apvPermYmd>
영업상태명 SALES_STATUS - STRING
폐업일자 - CLOSED_DATE  - DATE
소재지 도로명주소 - HSOP_ROADNAME_ADDRESS VARCHAR
소재지 우편번호 - HSOP_POSTNUM NUMBER
WGS84 위도 - A_LATITUDE double
WGS84 경도 - A_HARDNESS double

CSV 파일에서 해당하는 속성만 추가 
폐업일자는 DATE로 바꿔줘야됨 기본값 VARCHAR2로 되어있어서 안바꾸면 오류


테이블 추가

ALTER TABLE ANIMALHOSP_MAIN ADD (
ANIMALHOSP_PIC VARCHAR2(255), 
ANIMALHOSP_OPEN VARCHAR2(255), 
ANIMALHOSP_REST VARCHAR2(255), 
STAR_TOTAL NUMBER, 
RV_CNT NUMBER,
HSOP_PHONENUM VARCHAR2(20)
SID VARCHAR2(50)
);


PK 설정
ALTER TABLE ANIMALHOSP_MAIN
ADD CONSTRAINT PK_ANIMALHOSP_MAIN PRIMARY KEY (ANIMALHOSP_NO);

ALTER TABLE NOTE_MAIN
ADD CONSTRAINT PK_NOTE_MAIN PRIMARY KEY (NOTE_NO);



외래키 설정

ALTER TABLE ANIMALHOSP_MAIN
ADD CONSTRAINT fk_staff_id
FOREIGN KEY (sID)
REFERENCES STAFF(sID);


시퀸스 
CREATE SEQUENCE ANIMALHOSP_MAIN_SEQ
    START WITH 9049
    INCREMENT BY 1;

CREATE SEQUENCE NOTE_SEQ
  START WITH 1
  INCREMENT BY 1;
